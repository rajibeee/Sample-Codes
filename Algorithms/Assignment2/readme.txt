Need to install pip, matplotlib, networkx,

sudo apt-get install python-pip
sudo pip install networkx
sudo apt-get install python-matplotlib


This is needed if the PC does not have display , Before importing pyplot>>
import matplotlib 
matplotlib.use('Agg')

- Run the attached script
- the images will be generated where the python code is
